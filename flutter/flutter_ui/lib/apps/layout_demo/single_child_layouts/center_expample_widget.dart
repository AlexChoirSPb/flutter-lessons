import 'package:flutter/material.dart';

class CenterExpampleWidget extends StatelessWidget {
  const CenterExpampleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Placeholder(
        color: Colors.blue,
      ),
    );
  }
}
